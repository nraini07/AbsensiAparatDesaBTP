<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp"
    android:background="#FFFFFF"
    android:layout_marginBottom="4dp">

    <!-- Baris: Nama | Tanggal | Jam | Catatan -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- Nama -->
        <TextView
            android:id="@+id/tvNamaRiwayat"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="Nama Pegawai"

            android:textStyle="bold"
            android:textSize="14sp"
            android:textColor="#000000" />

        <!-- Tanggal -->
        <TextView
            android:id="@+id/tvTanggalRiwayat"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.5"
            android:text="00-00-0000"
            android:textSize="12sp"
            android:textColor="#000000" />

        <!-- Jam -->
        <TextView
            android:id="@+id/tvJamRiwayat"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="00:00"
            android:textSize="12sp"
            android:textColor="#000000" />

        <!-- Catatan jenis absensi -->
        <TextView
            android:id="@+id/tvCatatanJenis"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.5"
            android:text="Masuk"
            android:textSize="12sp"
            android:textColor="#000000"
            android:gravity="center_horizontal" />
    </LinearLayout>

    <!-- Baris keterangan (di bawah) -->
    <TextView
        android:id="@+id/tvKeteranganRiwayat"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Keterangan tambahan"
        android:textSize="12sp"
        android:textColor="#000000"
        android:layout_marginTop="2dp" />

    <!-- Baris lokasi / status area tugas -->
    <TextView
        android:id="@+id/tvLokasiRiwayat"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Lokasi: ..."
        android:textSize="12sp"
        android:textColor="#000000"
        android:layout_marginTop="2dp" />

</LinearLayout>